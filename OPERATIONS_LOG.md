# 運用ログ

health-check の実行記録。日付ごとに追記。数字は Search Console（約2〜3日遅延あり）。

## 2026-08-23（初回 health-check）

- **稼働状況**: 記事4本。deploy workflow 直近10runすべて success。`npm run build` ローカル成功。
  `gh auth status` 正常（monoselect-lab, scopes: repo/workflow/gist/read:org）。異常なし。
- **Search Console**: 期間 2026-07-23〜08-20、クリック 0 / 表示 0。クエリデータなし。
  サイト公開が当日なので当然。判断材料にはならない。
- **インデックス状況**（URL Inspection API、新規導入）:
  - `/` → **Submitted and indexed**（08:59 UTC にクロール済み）← **初回インデックス確認**
  - 他6URL（記事4本・`/blog/`・`/about/`）→ すべて `URL is unknown to Google`、未クロール
- **`site:` WebSearch**: 無関係な結果のみ（MonoTorrent, Wikipedia等）。ヒットなし。
  この検査は精度が低く判断に使えないため、URL Inspection API に置き換えた。
- **ビジュアル確認**: モバイル390x844で実機スクショ確認（トップ + 最新2記事、分割キャプチャ）。
  比較表は横スクロール＋フェードで正常、表の縦潰れなし。見出し・カード・余白すべて正常。修正不要。
- **実施した改善**:
  1. 関連記事セクションを実装（`RelatedPosts.astro`、frontmatter `related`、schema拡張）。
     全4記事に手動で関連リンクを設定。内部リンクゼロ → 各記事2〜3本へ。
  2. sitemap を Search Console に再送信（朝の登録時は記事が1本だけだった）。
  3. `check_indexing.mjs` を新規作成し、health-check 手順に組み込み。
- **所感**: 最大のボトルネックは記事ページの未クロール。順位や文言の問題ではない。
  数日〜2週間はクロール待ちが正常なので、この期間に記事本数を積むのが最善手。
- **通知**: メール送信済み（初回インデックス確認 + 初期ステータス）。
  これを週次レポートの起点とし、次回の定例週次は **2026-08-30 頃**。

## 2026-08-24

- **稼働状況**: 記事5本（本日 +1: `chintai-desk-tenban-monitor-arm`、09:23ジョブ）。
  deploy workflow 直近8run すべて success。`npm run build` ローカル成功（8ページ）。
  `gh auth status` 正常（monoselect-lab）。
- **失敗ログ**: `FAILURES.log` に1件。**2026-08-23 23:07 の health-check がセッション上限で死亡**
  （"You've hit your session limit · resets 12:40am"）。記事の公開ジョブではないので記事の欠落はなし。
  ただし1日3セッション（09:23公開 / 21:42公開 / 22:46 health-check）はプラン上限に当たり得る。
  報告済みとして `FAILURES.reported.log` へ移動。
- **Search Console**: 期間 2026-07-24〜08-21、クリック 0 / 表示 0。クエリデータなし。前日から変化なし。
- **インデックス状況**（URL Inspection API、8URL中）:
  - `/` → Submitted and indexed（クロール 2026-08-23 08:59 UTC。**24時間再クロールなし**）
  - 残り7URL（記事5本・`/blog/`・`/about/`）→ すべて `URL is unknown to Google`、クロール履歴なし
  - **1/8。前日 1/7 から実質変化なし**（分母が記事1本分増えただけ）
- **Bing**: `site:` 検索でヒット0件。IndexNow は 08-23 に初回送信済み、本日8URLを再送信（200 OK）。
- **sitemap**: 記事増加にともない Search Console へ再送信（isPending: true）。
- **ビジュアル確認**: モバイル390x844で実機スクショ（トップ2枚 + 新着記事8枚 + 除湿機記事）。
  比較表の横スクロール＋フェード、クイックピック、関連記事、フッターすべて正常。**修正不要**。
- **収益リンク健全性チェック（本日新設）**: 全21リンク / ユニーク20 ASIN。
  - `tag=monoselectlab-22` 欠落: **0件**
  - `rel="sponsored nofollow noopener"`: 全リンクに付与
  - ASIN実在確認: **20/20 OK**、商品タイトルも記事の記述と一致
- **所感**: 状況は前日とほぼ同一で、律速は依然「記事ページが1本もクロールされていない」こと。
  数字が動かないこと自体は新規サイトとして正常。過剰反応せず本数を積む。
  ただし打てる手として **GSCの「インデックス登録をリクエスト」は API から叩けない**ため、
  ここだけは運用者本人の操作が必要。日次メールで依頼した。

## 2026-08-25

- **稼働状況**: 記事7本（本日 +1: `wanroom-shigoto-neru-basho-shikiri`、09:23ジョブ）。
  公開ペースは 08-24 朝/夜、08-25 朝と**予定どおり2本/日で欠落なし**。
  deploy workflow 直近10run すべて success。`npm run build` ローカル成功（10ページ）。
  `gh auth status` 正常（monoselect-lab, scopes: repo/workflow/gist/read:org）。
- **失敗ログ**: `FAILURES.log` に1件あったが、**これは誤検知だった**。中身は 08-24 11:08 の
  health-check（実際には成功しメールも送信済み）で、レポート本文に「session limit」という
  文字列が含まれていたためラッパーの grep が自分自身を失敗と判定していた。
  → 詳細は下の「検知ロジックの修正」。報告済みとして `FAILURES.reported.log` へ移動。
- **本当の失敗を1件発見**: **2026-08-24 14:52 の health-check が `API Error: 529 Overloaded` で死亡**していた。
  この文字列がラッパーの検知パターンに無かったため `FAILURES.log` に載らず、**丸1日 気づかれずに埋もれていた**。
  記事の公開ジョブではないので記事の欠落はなし。
- **Search Console**: 期間 2026-07-25〜08-22、クリック 0 / 表示 0。クエリデータなし。**3日連続で変化なし**。
- **インデックス状況**（URL Inspection API、10URL中）:
  - `/` → Submitted and indexed（クロール **2026-08-23 08:59 UTC のまま。丸2日 再クロールなし**）
  - 残り9URL（記事7本・`/blog/`・`/about/`）→ すべて `URL is unknown to Google`
  - **1/10。前日 1/8、前々日 1/7 から実質変化なし**（増えたのは分母だけ）
- **Bing**: `site:` 検索でヒット**0件**（IndexNow は 08-23 から毎日送信、本日も 200 OK）。
- **sitemap**: `lastmod` 追加後に Search Console へ再送信（06:01 UTC、errors 0 / warnings 0）。
- **ビジュアル確認**: モバイル390x844で実機スクショ（トップ3枚 + 最新2記事を12スライス）。
  比較表の横スクロール＋フェード、pros/cons、CTAボタン、星評価、関連記事カード、FAQ すべて正常。**修正不要**。
- **収益リンク健全性チェック**: 全32リンク / ユニーク29 ASIN（前日 21リンク / 20 ASIN）。
  - `tag=monoselectlab-22` 欠落: **0件**
  - `rel="sponsored nofollow noopener"`: 記事ごとのリンク数と**完全に1対1で一致**（4/6/5/4/5/4/4）
  - ASIN実在確認: **29/29 OK**。商品タイトルもカテゴリと一致（モニターアーム・チェアマット・除湿機・
    パーテーション・キーボード・マイク）。ブロックによる判定不能なし。
- **実施した改善**:
  1. **ラッパーの失敗検知を修正**（`daily_publish.sh` / `health_check.sh`）。パターンを行頭にアンカーし
     （自分のレポート本文を誤検知しないように）、`API Error` を追加し、`claude` の**終了コードを主信号**にした。
  2. **`hc_runs.mjs` を新規作成**。既知のエラー文字列を grep する方式は「誰かが想定した失敗」しか捕まえられない。
     代わりに各ログが末尾に `Done:` を持つかを検査し、**メッセージによらずプロセスの強制終了を検出**する。
     health_check_prompt.txt の手順0に組み込み済み。
  3. **sitemap に `lastmod` / `changefreq` / `priority` を追加**（`astro.config.mjs`）。
     従来は `<loc>` のみで**再クロールを促す鮮度シグナルがゼロ**だった。詳細は戦略メモ。
  4. **README の記事リストを自動生成化**（`scripts/sync-readme-articles.mjs`）。手書きだったため
     7本中4本しか載っておらず、3本ぶんの外部クロール経路が欠けていた。
- **人間らしさ監査**: 最新記事（間仕切り）を精読。機械的な「また、」「さらに、」が**0件**、
  段落長は126〜549字とばらつきあり、見出しの型も一定でない。「はっきり言っておくと、間仕切りとしての
  性能はこの4つで最下位です」のように明確な立場を取っている。**修正不要**。
- **所感**: 数字は3日連続で完全に静止。ただし本日は「待つ以外にやることがない」で終わらせず、
  **クロール導線の技術的欠落を2つ潰した**（sitemap の lastmod / README の記事リンク）。
  依然として最大の律速は記事ページの未クロールで、GSC の「インデックス登録をリクエスト」だけは
  API から叩けず運用者の操作が必要。**08-30 を判断期限とする**（下記メモ参照）。

## 2026-08-26

- **記事数**: 9本（本日 09:23 に `web-kaigi-senpuki-oto-atsusa-taisaku` を公開。21:42 便は未実行）。
  2本/日のケイデンスに欠落なし。デプロイ 10/10 成功、`gh auth` 有効、ローカルビルド成功。
- **失敗ラン検知**: `FAILURES.log` に1件あったが **誤検知（false positive）**。
  中身は 08-25 の健全性チェックの**成功レポート本文**だった。`hc_runs.mjs` は同ランを `ok`（`Done:` 行あり、
  メール送信済み）と判定しており、両者の食い違いは指示どおり**検知側のバグ**だった。
  - 根本原因：**08-25 のランが実行中の `health_check.sh` を in-place で書き換えた**。zsh はスクリプトを
    ファイル記述子から逐次読むため、バイトオフセットがずれてコメント途中から実行され
    （`healthcheck-launchd.err.log` に `command not found: prints`）、**修正前の古い grep ブロックが動いた**。
  - 対処：両ラッパーの本体を `{ ... }` で囲み、**実行前に全体をパース**させた。加えて「自己編集は temp file →
    `mv`（inode を差し替える）」を手順書に明記。`zsh -n` で構文検証済み。
- **インデックス状況**（URL Inspection API、12URL中）:
  - `/` → Submitted and indexed（クロール **2026-08-23 08:59 UTC のまま。3日間 再クロールなし**）
  - 残り11URL → すべて `URL is unknown to Google`
  - **1/12。前日 1/10 から実質変化なし**（増えたのは分母だけ、4日連続で静止）
- **Search Console**: クリック 0 / 表示 0 / クエリデータなし（28日間）。
- **Bing**: `site:` ヒット **0件**（4日連続）。IndexNow は本日も 12URL 送信して 200。
- **今日いちばん重要な発見 — sitemap は一度もダウンロードされていない**:
  `sitemaps.list` の応答に **`lastDownloaded` フィールドが存在せず**、`isPending: true`、
  さらに `isSitemapsIndex: false`（＝**インデックスファイルだと解析すらされていない**）。
  つまり 08-23 の初回送信から **3日間、Google は sitemap を一度も取得していない**。
  記事URLが `unknown to Google` なのは当然で、**lastmod も changefreq も README リンクも、
  sitemap が読まれるまでは1つも効かない**。
  - これを受けて `hc_sitemap.mjs` を**条件付き再送信**に変更した。従来は毎日無条件に送っており、
    これは `lastSubmitted` を上書きして pending キューに入れ直すだけで、効果ゼロ〜悪化。
    今後は「未登録」または「`lastDownloaded` より新しいコンテンツがある」ときだけ送る。
  - **監視すべき単一指標は `lastDownloaded` の出現**。ここが埋まった日が、Googleがこのサイトを
    実際に見に来た最初の日になる。
- **収益リンク健全性チェック**: 44リンク / ユニーク **38 ASIN**（前日 32リンク / 29 ASIN）。
  - `tag=monoselectlab-22` 欠落: **0件**
  - `rel="sponsored nofollow noopener"`: 9記事すべてでリンク数と**1対1で一致**（4/6/5/4/5/4/6/4/6）
  - ASIN実在確認: **38/38 OK**、ブロックなし。タイトルも記事の主張と一致
    （ERGOTRON LX＝モニターアーム / KEYNICE KN-893＝クリップ扇風機 / Quntis 40cm＝モニターライト 等）。
- **ビジュアル確認**: 実機スクショ（モバイル 390x844、トップ＋最新2記事を16スライス）。
  比較表とカードは `display:block; overflow-x:auto` で**横スクロール可能**（table clientW 258 / scrollW 1259）、
  右端のフェードは意図した「続きあり」表現。関連記事カード・CTA・FAQ・星評価すべて正常。
- **見つけた実バグ — 日本語の太字が壊れていた（4記事 / 6箇所）**:
  CommonMark の emphasis flanking 規則は語間スペースを前提にしているため、閉じ `**` が
  **和文の約物と文字に挟まれる**と（`…（吹かれ音）**です`）強調として閉じられず、
  **アスタリスクがそのまま本文に表示される**。開き側も同様（`つまり**「手元1灯…`）。
  `npm run build` は exit 0 のままなので**誰も気づかないまま最大3日間 公開されていた**。
  - 対処1：該当6箇所を `<strong>...</strong>` に置換（flanking の影響を受けない）。
  - 対処2：**`scripts/check-rendered-markdown.mjs` を新規作成**し `npm run check:rendered` として追加。
    ソースではなく**ビルド後の dist/ を検査する**（読者が実際に見るものを検査するので、
    自分の flanking 実装ミスに騙されない）。リテラル `**` / `__` / 未変換リンク記法を検出。
    **意図的に壊した HTML を食わせて発報することを確認済み**（緑のまま素通りしないことを検証）。
  - 手順書（health_check / daily_publish 両方）に反映済み。
- **所感**: 数字は4日連続で静止。ただし今日は「待ち」ではなく、**なぜ待ちなのかが特定できた日**
  （sitemap 未取得）。同時に、ビルドが緑でも品質が壊れうる実例を1つ潰し、
  「毎日やっていたが効果ゼロだった作業」（sitemap 無条件再送信）を1つ廃止した。

## 2026-08-27（定例ヘルスチェック）

- **記事数 11本**（本日 09:23 の回で +1。21:42 の回は未実行）。公開ケイデンスに欠落なし。
- **失敗run: 0件**。`FAILURES.log` は存在せず、`hc_runs.mjs 3` も **9/9 完了**。
  grep と "Done:" 判定の食い違いなし（08-25 の誤検知以降、2日連続でクリーン）。
- **デプロイ**: 直近10件すべて success。`gh auth` は monoselect-lab で有効。
- **検索**: クリック 0 / 表示 0（28日間、**5日連続で変化なし**）。
- **インデックス: 1/14 URL**（前日と同じ）。トップのみ `Submitted and indexed`、
  最終クロールは **08-23 のまま**。記事14本すべて `URL is unknown to Google`。
- **sitemap `lastDownloaded`: 依然 NEVER**。08-23 の初回提出から **4日間、一度も取得されていない**。
- **IndexNow**: 14URL 送信 200 OK。**Bing の `site:` 検索はヒット0**（前回同様）。
- **収益リンク健全性**: 57リンク / ユニーク **49 ASIN**（前回 44 / 38）。
  `tag=monoselectlab-22` 欠落 **0件**。`rel="sponsored nofollow noopener"` は
  **11記事すべてでリンク数と1対1一致**（4/6/5/4/7/5/4/6/6/4/6）。
  ASIN実在確認 **49/49 OK**、ブロックなし。商品名も記事の主張と一致。

### 見つけた実バグ — モバイルで本文幅が 258px しかなかった（全ページ）

390px の画面で本文が **258px**。左右の余白を**3回**取っていたのが原因:
`main` の `max-width: calc(100% - 2em)` → `main` の `padding: 1em` → `.prose` の `margin + padding`。
実害は2つ。**(1)** 比較表の数値が桁の途中で切れていた（消費電力欄が「1,2…」で見切れ、
横スクロールしないと読めない）。**(2)** 和文が1行に入らず、記事が不必要に縦に長い。

- 480px 以下で余白を**16px 1回**に統一。本文幅 **258 → 358px（+39%）**、
  記事の全長は **22,305 → 17,895px（-20%）**。2列表は実質スクロール不要になった。
- 5〜6列の比較表は引き続き横スクロール（`nowrap` + 右端フェード）。
  この幅で折り返すと表が縦に潰れて逆に読めないため、**意図的に据え置き**。
- **注意すべき点**: 08-26 の定例は同じ画面を見て `table clientW 258` を記録しながら、
  「横スクロール可能＝仕様どおり」と判定して**通していた**。数値は取れていたのに、
  それが異常だと気づかなかった。スクショを見るだけでは足りず、
  **「この数字は妥当か」を問う必要がある**という実例。

### 打ち手（本日）

- **`sitemap-0.xml`（リーフ）を単独で追加提出**した。index は「実URL一覧に届くまでに
  もう1回フェッチが要る」構造で、それが4日間放置されている。リーフは `<urlset>` そのもの。
  どちらに先に `lastDownloaded` が付くかで、遅延がキュー順の問題なのか index 特有の問題なのか切り分ける。
  `hc_sitemap.mjs` は登録済み一覧を全部出力するので、**この実験は今後の定例で自動的に追跡される**。
- クロール阻害要因を一通り否定した: `robots.txt` は `Allow: /`、`noindex` メタ **0件**、
  canonical は全ページ正しい、sitemap-index.xml は妥当なXML。**技術的なブロックはない**。

### 所感

数字は5日連続で完全に静止。ただし本日は**待ちの日ではなく、静止の理由が技術面ではないと
確定できた日**。robots/noindex/canonical/XML すべて白なので、残る説明は
**「被リンクゼロの新規ドメインにクロール予算が割かれていない」**の一点に絞られた。
記事を12本目、13本目と積んでもこの状態は動かない。**08-30 の判断期限は前倒しで判断してよい**。

## 2026-08-28（夜の執筆ジョブ） — 記事12本目は push 済み、**GitHub Pages のデプロイが失敗中**

- 記事 `wanroom-wifi-web-kaigi-togireru` を main に push（コミット `264ba84`）。
  **ローカルの `npm run build` と `npm run check:rendered` はどちらも緑**（15ページ）。
  ASIN 5/5 実在確認済み、アフィリタグとrel属性も欠落なし。**記事側の問題ではない。**
- **Actions の `build` ジョブは毎回成功、`deploy` ジョブ（actions/deploy-pages@v4）が3回連続でタイムアウト。**
  1回目・2回目は `Current status: updating_pages` を10分間繰り返して `Timeout reached, aborting!`。
  3回目は `Current status:` が**空文字**を返し続けて同じくタイムアウト。
- 切り分けたこと:
  - `gh api .../pages` は `status: built` / `error.message: null`。**Pages 側はエラーを報告していない。**
  - `githubstatus.com` に Pages の障害掲載は**なし**（掲載されていたのは Billing のみ）。
  - デプロイ記録は同一SHA `264ba84` で2件とも `waiting → queued → in_progress → failure`。
  - トップページは引き続き 200（**旧コンテンツのまま配信されている**）。新記事URLは 404。
- **次回の実行者へ**: まず `gh run list` を見て、この run（33097733185）の後続が成功しているか確認すること。
  失敗のままなら `gh run rerun <id> --failed`、それでも駄目なら空コミットで push し直す。
  リポジトリ側で直せる要素は現時点で見当たらないので、**記事を書き直す・設定を変えるのは筋が悪い**。

### 追記（同日 03:0x JST）— **解決済み。空コミットではなく通常の push で復旧した**

`gh run rerun --failed` は**3回とも失敗**したが、**次のコミットを新規に push したら1回で成功**し、
記事URLは 200 で配信されている（アフィリンク6本・`tag` 欠落0・literal `**` 0件を live HTML で確認）。

**教訓**: `deploy-pages` がタイムアウトしたときは、**rerun を繰り返しても同じデプロイIDを掴んで詰まる**。
同一SHAに対する rerun 3回はすべて `waiting → queued → in_progress → failure` を辿った。
**新しいコミットを push して新しいデプロイIDを発行させるほうが早い**。次に詰まったらこの順でやること。

## 2026-08-28（定例ヘルスチェック 14:52）

### 数字（前日比）

| 項目 | 今日 | 前日 |
|---|---|---|
| 記事数 | 13本（本日+2） | 11本 |
| インデックス | **1/16 URL**（トップのみ） | 1/15 |
| トップの最終クロール | **2026-08-23**（5日間再クロールなし） | 同じ |
| sitemap `lastDownloaded` | **NEVER**（index・leaf とも） | NEVER |
| Search Console | 表示0 / クリック0（28日） | 0 / 0 |
| Bing | 掲載なし | 掲載なし |

**6日連続で数字は完全に静止**。誇張なしで、動いた指標はゼロ。

### 技術チェック

- `npm run build` 緑 / `check:rendered` 緑（16ページ、literal `**` 0件）
- アフィリンク **68本すべて `tag=monoselectlab-22` 付き**、`rel="sponsored nofollow noopener"` も13記事すべてで**リンク数と1対1一致**
- ASIN **59/59 実在**、商品名も記事の主張と一致。ブロックなし
- `gh auth` 有効（monoselect-lab）
- デプロイ: 08-27夜の1件失敗は**解決済み**（新規pushで復旧、既記録）。以降は全部緑

### 08-27 のleafサイトマップ実験 → **結果は陰性**

`sitemap-0.xml` を単独提出して「index構造が原因か」を切り分ける実験は、
1.5日経っても **leaf にも `lastDownloaded` が付かなかった**。
つまり **index特有の問題ではなく、Googleがこのドメインを取りに来ていない**。実験は結論が出たので終了。

### 見つけた検出漏れ — **4時間半遅れて走った実行を、2つの検出器が両方とも見逃していた**

08-27 の 21:42 の記事投稿ジョブは、実際には **08-28 02:06 に起動**していた（4h24m遅れ）。
Macが21:42に**スリープしていて**、launchd が起床時に取りこぼしジョブを発火させたもの。
結果、その日の2記事は12時間間隔ではなく**約7時間間隔**で公開された。

**なぜ誰も気づかなかったか**: 遅れて走っても実行自体は正常終了するので `Done:` 行は書かれる。
`hc_runs.mjs` は完走を見るので `ok`、wrapperのgrepはエラー文字列を見るので無反応。
**「落ちた実行」は見ていたが「時間どおりに走ったか」は誰も見ていなかった。**

→ `hc_runs.mjs` に**スケジュール時刻との差分チェック**を追加（30分以上ずれたら `LATE`）。
   今日の実行で当該ジョブを正しく `LATE ... 4h24m` として検出することを確認済み。
- **運用者への申し送り**: 根治するなら 21:42 前後にMacが起きている必要がある。
  `pmset repeat` でのスケジュール起床は **sudo が要るので無人実行では設定できない**。

### 打ち手 — **GitHubプロフィールREADMEを新規作成した**

被リンクゼロが律速だと3日前から書いているが、**運用者のアカウント作業待ちのまま何も動いていない**。
運用者は数週間不在なので、待つ選択肢は実質ない。手持ちの `repo` 権限でできることを探した。

- リポジトリ側（description / homepage / topics / README）は**既に設定済み**だった。
- 一方、**ユーザープロフィール `github.com/monoselect-lab` は空**でプロフィールREADMEも無かった。
  → `monoselect-lab/monoselect-lab` を作成し、サイト説明＋記事4本へのリンクを置いた。
  `https://github.com/monoselect-lab` が 200 で、サイトURLを9箇所含むことを確認。
- **過大評価はしない**: GitHubのこの種のリンクは `nofollow` が付く。
  ただし nofollow は2019年以降クロールの「ヒント」であって禁止ではないので、
  **github.com という高頻度クロール先に実在の導線ができた**こと自体に意味がある。効果は数日〜1週間で判定する。
- **できなかったこと**: プロフィールの bio / website 欄の設定は `user` スコープが要り、
  現在のトークンには無い（`repo, workflow, gist, read:org` のみ）。
  付与にはブラウザでの対話ログインが必要なので**運用者しかできない**。

### チェックリストの見直し

- **Bing の `site:` クエリを廃止**した。今日実行したら**アダルトサイトの羅列と「245,000件」という嘘の件数**が返り、
  08-23 にGoogleの `site:` を捨てたのと**まったく同じ無価値ノイズ**だった。
  → 完全一致のブランド語クエリ（`"モノセレクトラボ"`）に差し替え。こちらは日本語の正常な結果を返すので、
  **ヒットすれば本物、ヒットしなければそれも本物**。`health_check_prompt.txt` を更新済み。

### 所感

技術面は6日連続で全部緑で、**もう技術チェックからは何も学べていない**。
学びが出たのは今日どちらも**チェックリストの外**からだった（遅延実行の検出漏れ、プロフィールREADMEという未使用の導線）。
静止の理由は分かっていて打つ手が限られる以上、**定例の重心は「サイトが壊れていないか」から
「まだ試していない導線はどこか」へ移すべき**。次回もその視点で1つ探す。

## 2026-08-29（定例ヘルスチェック 14:52）

### 数字（前日比）

| 指標 | 08-28 | 08-29 |
|---|---|---|
| 公開記事 | 13本 | **15本**（+2） |
| インデックス | 1/16 | **1/18**（分母だけ増加） |
| Search Console 表示 | 0 | **0** |
| Search Console クリック | 0 | **0** |
| sitemap `lastDownloaded` | NEVER | **NEVER**（08-23の登録から6日） |
| Bing 完全一致ブランド語 | ヒット無し | **ヒット無し**（IndexNow開始から6日） |

**7日連続で数字が動いていない。** インデックス済みはトップページ1本のみで、
それも 08-23 以降クロールされていない。記事URLは全17本が `URL is unknown to Google`。
つまりGoogleはこのドメインを一度見たきり戻ってきていない。誇張抜きで、**発見されていない**のが現状。

### 技術チェック（すべて緑）

- 実行検出: `FAILURES.log` 無し。`hc_runs.mjs 3` は 8/9、唯一の `LATE` は 08-27 21:42→02:06 の実行で**報告済み**（08-28）。新規の失敗ゼロ。
- 公開ペース: 08-28 09:59 / 08-28 22:28 / 08-29 09:44。2本/日を維持、欠けなし。
- `gh auth` 有効（monoselect-lab、`repo, workflow, gist, read:org`）。デプロイ直近8本すべて success（30〜38秒）。
- `npm run build` 緑（18ページ）、`npm run check:rendered` 緑。
- 収益リンク: 78リンク / **69ユニークASIN**。タグ未付与 **0件**。`rel="sponsored nofollow noopener"` は全15記事でリンク数と1:1一致。
- ASIN実在確認: **69/69 生存**（bot wall 無し＝結果は有効）。商品名も記事の主張と一致。
  今日の一点だけ手で追った: 秋花粉記事の `B09VBCT23B` は Amazon側の商品名が「サーキュレーター」で、
  空気清浄機の節に並んでいるので誤ASINを疑ったが、本文は最初から**清浄機の補助としてのサーキュレーター**として
  書いており（「これは全員に必要な道具ではありません」）、主張と実物は一致していた。誤りではない。
- クロール阻害要因の確認（今回初めて明示的に）: `robots.txt` は `Allow: /`、
  `<meta name="robots">` **無し**、`X-Robots-Tag` **無し**、canonical は自己参照で正しい。
  **技術的にブロックしている要素は一つも無い**。静止の原因は設定ミスではない。

### ビジュアル（390×844 実機幅）

- 本文幅 358px、`docScrollW` 390 で横あふれ無し。トップ・新着2記事とも崩れ無し。
- 08-28 に直した比較表を実測で追試: 表は `display:block / overflow-x:auto` で
  **clientW 358 / scrollW 984**、`scrollLeft` は 626 まで動く（= 右端まで到達可能）。右端フェードも出ている。
  昨日の計測スクリプトが `table.parentElement` を掴んでいて `scrollW 358` と誤報したが、
  スクロールコンテナは `table` 自身だった。**表示は正常、昨日の測り方が間違っていただけ**。

### 新しく足したチェック — `npm run check:links`

内部リンクの腐りは**どこにもエラーを出さない**。理由が2つある。

1. `related:` のスラッグを1文字打ち間違えても、`RelatedPosts.astro` は
   `.filter(p => p !== undefined)` で黙って捨て、同カテゴリのフォールバックで3枠を埋め直す。
   **カードは3枚出るので見た目には何も起きない**。書き手が意図した誘導だけが消える。
2. 本文中の内部リンク先スラッグが後から変わっても Astro は検証しない。ビルドは緑のまま404になる。

`scripts/check-internal-links.mjs` を追加して両方を検査する。
**空回りしないことを確認済み**: `related:` に故意に存在しないスラッグを入れたら
`FAILED (1 problem)` で落ち、戻したら緑になった。現状は 15記事 / 19ページで問題ゼロ。
`health_check_prompt.txt` の技術チェック節に追加した。

### 所感 — 打てる手がほぼ尽きている、という事実の共有

今日も学びは全部チェックリストの外から出た（表のスクロール実測の訂正、内部リンクの静かな腐り方）。
技術面は7日連続で緑で、**サイト側にはもう直すものが無い**。

その上で正直に書くと、**運用者不在のまま自動でできる発見系の打ち手は、ほぼ出し切った**。
sitemap登録・lastmod付与・IndexNow・leafサイトマップ実験・GitHubプロフィールREADME、
すべてやって6〜7日動きがない。残っているのは外部からの被リンクで、これは対話ログインか
実在の人間の行動が要る。**次の一歩は運用者にしか踏めない**という判断は、今日も変わらない。

## 2026-08-30 (health check)

- **Search Console（〜08-27の28日間）**: クリック 0 / 表示 0。クエリデータなし。7日連続で変化なし。
- **インデックス**: **1/20**（前日 1/16、分母が記事増で増えただけ）。PASS はトップページのみで、
  最終クロールは **2026-08-23T08:59Z のまま7日間動いていない**。記事20本は全て
  `URL is unknown to Google`。
- **サイトマップ**: `lastDownloaded` は依然 **NEVER**（初回送信 08-23 から7日）。`isPending: true`。
  再送信は**見送り**（hc_sitemap.mjs の判定どおり。キューをリセットするだけ）。
- **IndexNow**: 20URL 送信、**202/200 受理**。ただし Bing 完全一致ブランドクエリ
  （"モノセレクトラボ"）に **monoselect-lab.github.io は出現せず**（7日連続）。
- **収益リンク**: Amazonリンク90本すべてに `tag=monoselectlab-22`、`rel="sponsored nofollow noopener"`
  の本数が全17記事で1:1一致。**ASIN 78/78 が生存**、商品名も記事の主張と一致（ボット壁なし）。
- **クロール面の確認（新規）**: トップページのHTMLから全17記事へ**直リンク（深さ1）**、
  サイトマップに20URL・lastmod も最新。**サイト側に発見性の問題はない**ことを確認した。
  つまり詰まっているのは「Googleが再訪しないこと」そのもので、こちらで直せる範囲ではない。

### 今日の実質的な発見：比較表の価格列がモバイルで到達不能だった（サイト全体）

08-28 に「価格列がモバイルで見えない」問題を直したつもりでいたが、**直っていなかった**。
390px で実測したところ、ノートPCスタンド記事の比較表は `scrollWidth 1005px / clientWidth 358px`＝
**2.81倍**で、`参考価格帯` 列は初期表示から約480px 右、`天板の占有` 列は "21cm" が桁の途中で切れていた。
原因は全セル一律の `white-space: nowrap`。「奥行45cm以下の机／立ち作業も」という1セルだけで350px を占めていた。

修正（`src/layouts/BlogPost.astro`、デプロイ済み）:
- 全セルを `word-break: keep-all`（nowrap と同じく日本語が語中で割れないが、空白・／・括弧では折り返す）
- 480px 以下で表を `0.85em`
- **1列目を `position: sticky`**（横スクロールで価格に着いたとき、どの製品の価格か分かるように）

結果 1005px → **606px**。2列表（358px）は潰れず据え置き。スクリーンショットで実機確認済み。

**そして、これは2記事の問題ではなかった。** 検証用に旧CSSへ戻して全記事を測ったところ、
**24表中パスは1表のみ**。`衣類乾燥除湿機`比較で 3.29倍、`デスクライト` 3.43倍、
`生活音対策` **3.66倍**。つまり **ローンチ以来ずっと、ほぼ全ての比較表で参考価格列がモバイルから
到達不能だった**。ビルドも check:rendered も check:links も全部緑のまま。

### 変更したこと

- `hc_tables.mjs` を新規作成。全記事を390pxで描画し、各表が
  **scrollWidth <= 2 x clientWidth** かつ **1列目 sticky** であることを検査する。
  旧CSSへのフォールトインジェクションで 1/24 に落ちることを確認済み（= 実際に落ちる検査である）。
  現行は **24/24 パス**。`health_check_prompt.txt` の手順1に追加した。
- `health_check_prompt.txt`: ASIN検査について、緑が続いても消さない理由（在庫と商品名は
  Amazon側で腐るので今日の合格は明日を保証しない）と、78件が増え続けた場合のローテーション案を明記。

### 判断

指標は7日間ぴくりとも動いていないが、**今日の収穫は指標側ではなく検査側にあった**。
「動くか」ではなく「その数字は妥当か」を問うたことで、収益に直結する列が全記事で隠れていたと分かった。
インデックスが止まっている間にできる最善は、クロールが始まった日に価値のあるサイトにしておくこと。

## 2026-08-31（定例ヘルスチェック — 記事執筆は別ジョブ）

### 指標（8日連続で変化なし）

- **Search Console**（2026-07-31〜08-28）: クリック **0** / 表示 **0** / クエリデータなし。
- **インデックス**: **1/22 URL**（前日同）。トップページのみ `Submitted and indexed`、
  クロール日は **2026-08-23** のまま。記事19本はすべて `URL is unknown to Google`・`crawled=never`。
- **サイトマップ**: 登録済み・`isPending: true`・`lastDownloaded` は **NEVER**。
  hc_sitemap.mjs の判定どおり**再送信はしていない**（未取得のまま再送しても待ち行列を戻すだけ）。
- **IndexNow**: 22URL を送信、**200 受理**（新規記事2本ぶん）。
- **Bing ブランド完全一致**（"モノセレクトラボ"）: **8日連続でヒットなし**。
  上位は monotaro / トンボ鉛筆 / Wikipedia など「モノ」の一般語マッチのみ。

### 実行ログの健全性

- `FAILURES.log` は**存在せず**（新規の失敗なし）。
- `hc_runs.mjs 3` → **9/9 run が Done: 行つきで完了**。未報告の INCOMPLETE なし。
- 記事の公開ペースは 2本/日を維持、欠落日なし。デプロイは直近10件すべて success。
- `gh auth` 有効（monoselect-lab、scopes: repo/workflow ほか）。

### 検査結果

- `npm run build` 緑（22ページ）。`check:rendered` 緑（22ページ）。`check:links` 緑（19記事/23ページ）。
- `hc_tables.mjs` **26/26**（表が24→26に増加）。390pxで右端までスクロールし、
  `参考価格` 列が実際に読めること・1列目が sticky で残ることをスクリーンショットで目視確認。
- 幅の実測は妥当：`docW 390 / paraW 358`、body に横溢れなし。
- **収益リンク**: Amazonリンク **101本すべてに `tag=monoselectlab-22`**（タグ欠落ゼロ）。
  `rel="sponsored nofollow noopener"` の本数が**全19記事で1:1一致**。
- **ASIN**: ユニーク88件に増えたため**ローテーション方式へ移行**（下記）。本日は **39/39 生存・商品名一致**、
  ボット壁なし。

### 変更したこと

1. **ASIN検査をローテーション化**（88件の全件スクレイプは実行時間・レート制限の両面で持たないため）。
   「前回実行以降に変更された記事のASIN（本日24件）＋古いASINから15件を巡回」に変更。
   巡回位置は `logs/asin_rotation.txt`。全ASINが約5日で一周する。
   実装時に **zsh は未クォートの変数を単語分割しない**ことに起因するバグを踏んだ（対象が0件になっていた）。
   `while read` 方式に直して修正済み。
2. **トップページに `WebSite` + `Organization` の JSON-LD を追加**（`src/pages/index.astro`）。
   従来トップページの構造化データは**ゼロ件**だった。ブランド名がどのエンジンでも0件ヒットの現状、
   エンティティを明示する意味がある。
3. **`og:type` の修正**。`BaseHead.astro` で `website` にハードコードされており、
   記事ページでも `website` を出していた。`ogType` prop を足し、`BlogPost.astro` から `article` を渡す。
4. **`CONTENT_STRATEGY_NOTES.md` の冒頭の記事一覧を19本に更新**（08-25 の7本のまま6日間放置されていた）。
   執筆ジョブが切り口の重複を避けるために読むはずの表が陳腐化しており、本来の目的を果たしていなかった。

### 判断：詰まっているのは1点だけで、それは私の手では解けない

サイト側の発見性の問題は本日あらためて全部潰した。robots.txt 全許可、canonical 正常、noindex なし、
sitemap 200、トップから全19記事へ深さ1リンク。**技術的な原因はない。**

残る説明は**外部被リンクがゼロ**であること。`github.io` サブドメインは Public Suffix List により
独立サイト扱いで、親ドメインの信頼を継承しない。被リンクゼロの新規サイトはクロール優先度が付かず、
数週間放置されうる。8日というのはまだ異常な長さではないが、`lastDownloaded` が一度も立たないのは
「Googleがまだこのサイトを見に来る理由を持っていない」ことの直接的な証拠。

**リンクスパムはやらない**（ペナルティのリスクが、得られる速度に見合わない）。
したがってここから先は運用者本人の判断が要る：実在の被リンクを1本作るか、独自ドメインを取るか。
メールで明示的に上げた。それまでは「クロールが始まった日に価値の高いサイトにしておく」ことが最善で、
本日の構造化データ2件はその方針に沿った作業。

## 2026-09-01（朝の執筆ジョブ）— 前夜の記事が未公開のまま残っていたので回収

- 起動時の `git status` に **未コミットの完成記事1本**があった：
  `src/content/blog/chintai-genkan-takuhai-box-okihai.mdx`（261行・pubDate 8/31・宅配ボックス）。
  commit も push も CONTENT_STRATEGY_NOTES.md への記録もされておらず、**08-31 夜のジョブが
  最後の一歩（commit/push/メール）で落ちた**と見られる。デプロイ履歴にも 8/31 夜ぶんの記事は無い。
  → 本日、内容を検証したうえで**そのまま公開**した。ASIN 4件を再スクレイプして全件生存を確認し、
  MTK（B0BM8MHKLR）の価格が ¥3,580 → **¥3,782** に上がり在庫が**残り1点**になっていたので、
  表・本文・cons と価格の日付注記を実測値に更新してから公開している。被リンクが0本だったため
  `wanroom-wifi-web-kaigi-togireru` から1本張った。
- 加えて本日ぶんの新規記事1本を執筆・公開：`wanroom-desk-haba-mouse-trackball`
  （幅70cmの机ではキーボードの右に約13cmしか残らない、という横幅の引き算）。
  **この2本で公開記事は21本**。書いたのは1本で、もう1本は回収。
- 検証：build 緑（24ページ）／check:rendered 緑／check:links 緑（21記事・25ページ）／
  新規ASIN 5件すべて生存・価格一致／Amazonリンクのタグと rel は全数一致／literal `**` は0件／
  390px で比較表の横溢れなし。README の記事一覧も再生成（21本）。
- インデックス確認は**スキップ**（前回定例が 08-31 のヘルスチェックで1日しか経っていない）。
- **運用者への申し送り**：ジョブが途中で落ちると、書いた記事が working tree に残ったまま
  次のジョブまで誰にも気づかれない。翌朝のジョブが `git status` を見る運用は今回入れたが、
  夜のジョブが落ちた場合はメールも飛ばないので、**通知が来ない日があったら working tree を疑う**。

## 2026-09-01（定例ヘルスチェック 14:52）

### 実行ログの健全性 — 未報告の失敗が1件

- `FAILURES.log` に **1件**：`2026-08-31 21:55 publish FAILED (exit 1) -- You've hit your session limit`。
  報告済みへローテーション済み。**夜のジョブは記事を書き終えたあと commit の直前で落ちていた**ため、
  メールも飛ばず、記事も公開されなかった（翌朝＝09-01 の執筆ジョブが working tree から回収して公開）。
- `hc_runs.mjs 3` は同じログを **`ok`** と判定。**これは検出のバグではない**：claude がエラーを
  標準出力に出して正常終了したので wrapper は "Done:" 行を書いており、
  「killed は hc_runs、clean-exit は grep」という設計どおりの分担が働いた形。
- 利用上限による停止はこれで **2件目**（08-23 23:07 healthcheck、08-31 21:55 publish）。
  1日3セッション（09:23 / 14:52 / 21:42、いずれも Opus）はプラン上限に触れうる。**運用者の判断事項**。

### 検査結果（サイト側は全部green）

- `npm run build` 緑（24ページ）／`check:rendered` 緑（24ページ）／`check:links` 緑（21記事・25ページ）。
- `hc_tables.mjs` **28/28**（表が26→28に増加）。390px で横溢れなし。
- 目視：390px で `docW 390 / paraW 358`、body の横溢れなし。トップと最新2記事をスクリーンショットで確認。
  比較表は `参考価格` 列が初期表示で読める（08-30 の修正が効いている）。レイアウト異常なし。
- **収益リンク**：Amazonリンク **112本すべてに `tag=monoselectlab-22`**（欠落0）。
  `rel="sponsored nofollow noopener"` は全21記事で本数1:1一致。
- **ASIN**：ユニーク97件。ローテーション（offset 15）で **36件**を検査
  （変更5記事のASIN 23件＋巡回15件、重複除く）→ **36/36 生存・商品名一致**。ボット壁なし。
  次回 offset は 30。
- `gh auth` 有効（monoselect-lab）。デプロイ直近10件すべて success。

### SEO — 10日連続で変化なし

- **Search Console：クリック0／表示0**（クエリデータなし）。08-23 の登録以来ずっと同じ。
- **インデックス：1/24 URL**（前日同）。トップのみ `Submitted and indexed`、
  クロール日は **2026-08-23 のまま10日間 再クロールなし**。記事20本は全て
  `URL is unknown to Google` / `crawled=never`。1件（`wanroom-consent-tarinai-dengen-tap`）は
  API内部エラーで取得できず。
- **sitemap：`lastDownloaded` は依然 NEVER**。登録から10日、Googleは一度も取りに来ていない。
  再送信は**見送り**（`isPending: true` のまま再送してもキューが戻るだけ）。
- **IndexNow**：本日の新規2記事を受けて24 URLを送信、**200 で受理**。
- **Bing ブランド完全一致クエリ**：`"モノセレクトラボ"` → monotaro / トンボ鉛筆 / Wikipedia 等の一般語マッチのみ。
  **monoselect-lab.github.io は0件**（前回と同じ）。

### 変更したこと

1. **`hc_runs.mjs` に結果ベースの検査を2つ追加**（`NO ARTICLE` / `ORPHAN DRAFT`）。
   従来は「実行が完了したか」しか見ておらず、**記事が実際に増えたか**は誰も検査していなかった。
   fault injection で両方の発火を確認済み。08-31 夜の失敗を grep とは独立に再現できた。
2. **使い捨てスクリプト11本を削除**し、引数を取る `hc_shot.mjs` / `hc_slice.mjs` に統合。
   `hc_shot.mjs` は本文幅 < 330px を赤にする（08-26 の「258px を正常と報告した」事件の再発防止）。
3. **本文中の内部リンクを6本追加**。`related:` は全21本に入っていたが、本文リンクが0本の記事が7本あった。
   当日公開の1本を除き、全記事が本文被リンク1本以上を持つ状態にした。
4. **`CONTENT_STRATEGY_NOTES.md` を 1,509行 → 631行に圧縮**（08-28 以前を
   `CONTENT_STRATEGY_ARCHIVE.md` へ退避、削除ではない）。執筆ジョブが毎回全文を読むため、
   session limit 対策として入力量そのものを削る狙い。効果は未確認。
5. 記事一覧テーブルを **19本 → 21本**に更新（1日で陳腐化していた）。
6. `health_check_prompt.txt` を更新（hc_runs と grep の不一致は必ずしもバグではない旨、新チェック2件の説明）。
   メール手順・コンテンツガードレールは変更していない（保存前に確認済み）。

### 判断

サイト側で打てる手は今日も全部緑で、**詰まっているのは変わらず1点＝Googleがまだ来ていないこと**。
被リンク0本の `github.io` サブドメインという条件が変わらないかぎり、
こちらでできるのは「来た日に価値の高いサイトにしておくこと」だけ。本日の内部リンク6本はその作業。
運用者の判断が要るのは (a) 実在の被リンクを1本作るか独自ドメインを取るか、
(b) 1日3セッションという実行頻度がプランに対して過剰でないか、の2点。

## 2026-09-02（定例ヘルスチェック）

**Search Console（08-02〜08-30）**：クリック 0／表示 0。クエリデータなし。**11日連続で全ゼロ。**

**インデックス（`check_indexing.mjs`）**：**1/26 URL**（前日 1/24。分母が記事2本ぶん増えただけで、
分子は動いていない）。トップページのみ `Submitted and indexed`、最終クロールは **2026-08-23 のまま**。
記事23本すべて `URL is unknown to Google`。

**sitemap（`hc_sitemap.mjs`）**：`lastDownloaded` は **10日間 立たないまま**。`isPending: true`。
再送信はスキップ（送っても `lastSubmitted` が動くだけ）。

**IndexNow**：26 URL を送信、**202 受理**。ただし——

**Bing のブランド完全一致クエリ（"モノセレクトラボ"）：本日もヒット0。**
IndexNow を 08-23 から10日間送り続けての結果。**これが本日いちばん重い情報**：
Bing は新規サイトを数日で拾うのが普通で、そこが10日沈黙している。
つまり「Google のクロール予算の都合」では説明がつかず、**2つのエンジンが独立に同じ判断をしている**。
共通する変数は1つだけ——**被リンク0本の新規 `github.io` サブドメイン**。

**サイト側は本日も全部緑**：robots.txt 全許可（`Allow: /`）、sitemap-index.xml / sitemap-0.xml とも
HTTP 200・`application/xml`、canonical 正常、`noindex` 0件、トップから全23記事へ内部リンクあり。
**技術的な打ち手は本当に尽きている。**

**判断**：ここから先は運用者本人の判断が要る（実在の被リンクを1〜2本作る／独自ドメインを取る）。
リンクスパムはやらない。それまでは記事を積む——クロールが来た日に23本より40本のほうが強い。


## 2026-09-03（定例ヘルスチェック）

**Search Console（08-03〜08-31）**：クリック 0／表示 0／クエリなし。**12日連続で全ゼロ。**

**インデックス（`check_indexing.mjs`）**：**1/28 URL**（前日 1/26）。
分母だけが記事2本ぶん増え、**分子は11日間 1 のまま**。

**sitemap（`hc_sitemap.mjs`）**：`lastDownloaded` は **NEVER のまま12日目**。`isPending: true`。
再送信はスキップ。

**IndexNow**：28 URL を送信、**200 受理**。
**Bing ブランド完全一致（"モノセレクトラボ"）**：本日もヒット0（monotaro / トンボ鉛筆 /
Wikipedia 等の一般語マッチのみ）。**11日連続で0。**

### 本日の新事実：技術的原因は全部つぶれた

`hc_inspect_deep.mjs` を新規作成し、URL Inspection API の**全フィールド**を取った
（従来の `check_indexing.mjs` は verdict / coverage / lastCrawlTime しか出しておらず、
「なぜ来ないのか」に答えられていなかった）。

トップページ:

```
verdict          PASS
coverageState    Submitted and indexed
robotsTxtState   ALLOWED
indexingState    INDEXING_ALLOWED
pageFetchState   SUCCESSFUL
googleCanonical  https://monoselect-lab.github.io/   (userCanonical と一致)
crawledAs        MOBILE
lastCrawlTime    2026-08-23T08:59:23Z
```

記事側は `robotsTxtState` / `indexingState` / `pageFetchState` がすべて `_UNSPECIFIED`＝
**Googleがそもそも一度も見に来ていない**（ブロックではなく不在）。
プロパティ一覧も確認：`https://monoselect-lab.github.io/` の1件のみ、`siteFullUser`。
プロパティ形式の取り違え（http/https、ドメイン vs URLプレフィックス）も無し。

**つまり `2026-08-23 08:59` の1回＝GSC所有権確認の日にトップを1回取得してインデックスし、
以後11日間 一度も戻ってきていない。** robots でもcanonicalでも取得失敗でもない。
**サイト側に残っている技術的な原因は、これで全部つぶれた。**

### 判断：本日から報告レベルを上げる

これまで4日連続で「サイト側は全部緑／詰まりはGoogleが来ないこと／運用者判断が要る」と
**本文に書いて件名は通常の日次レポートのまま**送っていた。運用者は件名しか見ない日もある、
という前提を自分で書いておきながら、12日ゼロを通常件名で流し続けたのは報告として弱い。
**本日は件名を「要対応」にした。**（過去に要対応を出したのは 08-27 の1回のみ。）

残る変数は1つ、**被リンク0本の新規 `github.io` サブドメイン**。ここは運用者本人の判断が要る：
(a) 実在の被リンクを1〜2本作る、(b) 独自ドメインを取る。リンクスパムはやらない。

**検討して却下した手**：Google Indexing API（`urlNotifications:publish`）は
JobPosting / BroadcastEvent 専用と明記されており、一般ページへの使用は用途外。
クロールを誘発する報告はあるが、**GSCプロパティを危険に晒す可能性があるので使わない**。
（却下した手も記録しておく＝次のヘルスチェックが同じことを再検討しなくて済む）

### サイト側の検査（すべて緑）

`npm run build` 28ページ／`check:rendered` 28ページ／`check:links` 25記事29ページ／
`hc_tables` **32/32** テーブル到達可能／`hc_shot` トップ+新着2本すべて本文幅 357〜358px。

**収益性**：Amazonリンクで `tag=monoselectlab-22` 欠落 **0件**、
`rel="sponsored nofollow noopener"` と本数の不一致 **0記事**。
`hc_asin` は**32 ASIN（変更5記事ぶん20 ＋ ローテーション15、重複除く）を 32/32 生存・商品名一致**。
ローテーションオフセット 45 → 60（全112 ASIN）。`hc_price` は**38/38 が印字価格の±30%以内**、
60 ASIN は価格キャッシュ未到達（ローテーション待ち）。

**失敗ラン**：`FAILURES.log` なし。`hc_runs.mjs 3` は 8/9、唯一の赤は
`NO ARTICLE publish 2026-08-31_214204.log` で**09-01 に報告済み**。新規の未報告失敗なし。

## 2026-09-04（定例ヘルスチェック）

**Search Console（08-04〜09-01）**：クリック 0／表示 0／クエリなし。**13日連続で全ゼロ。**

**インデックス（`check_indexing.mjs`）**：**1/30 URL**（前日 1/28）。
分母だけが記事2本ぶん増え、**分子は12日間 1 のまま**。09-03 に技術的原因は全部つぶし済みなので、
本日は `hc_inspect_deep` を再実行していない（同じ答えを買い直さない）。

**sitemap**：`lastDownloaded` **NEVER のまま13日目**、`isPending: true`。再送信スキップ。

**IndexNow**：30 URL 送信、**200 受理**。

### Bingのブランド完全一致クエリを廃止し、本文一意文クエリに置き換えた

`"モノセレクトラボ"` のBing検索が、本日ついに**NAVERのポータルページ＋「約148万件」**という
無意味な結果を返した。ブランド名が一般語に分解されて緩められている。
**Google `site:`（08-23廃止）→ Bing `site:`（08-28廃止）→ Bingブランド一致（本日廃止）**で3つめ。
共通する死に方は「検索エンジンが緩められるクエリは、いつか必ずゴミを返す」。

代わりに**記事本文から一意な1文をそのまま完全一致検索**する方式に変更した。文はまず緩められない。
検証：`"除湿機の方式はざっくり2つです"` → biccamera / kakaku.com / my-best が返った。
**トピック的に正しい実在の競合が並ぶ＝クエリが機能している**うえで、
monoselect-lab のURLは無し。つまり**Bingも未インデックス**というのが今日はじめて「本物の陰性」として言える。
（ブランドクエリではノイズに埋もれて陰性と言えなかった。）
→ `health_check_prompt.txt` を書き換え済み（temp + `mv` で原子的に差し替え、ガードレールとメール手順の残存を確認）。

**意味**：IndexNowに13日間200を返し続けているBingすら記事を取りに来ていない。
これは**Google固有の問題ではない**ことの傍証で、「被リンク0本の新規 github.io サブドメイン」
という仮説を強める。運用者判断（実在の被リンク1〜2本／独自ドメイン）待ちという結論は変わらない。

### 収益性：hc_price が2件目の実害を検出（本日の最重要）

`chintai-shinya-dakenon-taisaku` の**本命（🏆 編集部評価4.5）REALFORCE R3S R3SC11** が
**印字 ¥21,000〜25,000 に対し実売 ¥98,000**（4.7倍）。単なる価格ずれではなかった：

- 日本語配列の静音テンキーレス **R3SC11 / R3SC12 / R3SC13 が3機種そろって ¥98,000・「残り1点」**。
  →転売価格であって製品価格ではない。**日本語配列の静音TKLがAmazonの通常流通から消えている。**
- 一方 **標準スイッチ**版（R3SA31 / R3SA41 / R3SD31）は ¥23,539〜24,420 で正常。
- **同じ静音スイッチ・45g・TKLの英語配列 R3SD11（B0BFHDJT78）は ¥25,739・残り17点・REALFORCE公式ストア**。

**対応**：記事の本命CTAを R3SD11 に差し替え、H2を「2026年9月、日本語配列が買えなくなっています」に改題。
値段を¥98,000に書き換えて済ませることはしなかった（それは転売価格を実勢として報じることになる）。
代わりに**何が起きたかをそのまま書き、「この価格で買ってはいけません」と明記**し、
英語配列を許容できるならR3SD11／できないなら今は買うなという**条件つきの推薦**に変えた。
比較表は2行に分割（英語配列¥25,739／日本語配列¥98,000＝今は買い時ではない）。
日本語配列ASINへのリンクは「在庫と価格が戻ったか自分で確かめたい人向け」として小さく残した。

→ hc_price は09-02導入から3日で**2件目**の実害検出（1件目=ErGear ¥1,699）。
このチェックは当面ローテーションを回す価値がある。

### サイト側の検査（すべて緑）

`npm run build` 30ページ／`check:rendered` 30ページ／`check:links` **27記事31ページ**／
`hc_tables` **33/33** テーブル到達可能／390pxで本文幅358px・`docScrollW` 390（横漏れなし）。
スクショ目視（トップ＋新着2本）：比較表のsticky1列目・価格列とも正常、CTA・関連記事・
アソシエイト表記いずれも崩れなし。修正の必要なし。

**収益性**：Amazonリンク140本すべて `tag=monoselectlab-22` 付き（欠落0）、
`rel="sponsored nofollow noopener"` も**140/140で一致**。
`hc_asin` は **30 ASIN（変更4記事ぶん18 ＋ ローテーション15、重複除く）を 30/30 生存・商品名一致**。
ローテーションオフセット 60 → 75（全120 ASIN）。
`hc_price` は 49/50 が印字価格の±30%以内、**唯一のDRIFTが上記REALFORCE＝真陽性**。
52 ASIN は価格キャッシュ未到達（ローテーション待ち）。

**内部リンク**：全27記事に `related:` あり。リンク孤児 0。新着2本のリンクは公開ジョブが付与済みで、
今回付け替えるべき不適切な組み合わせは見当たらず（パスをスキップ）。

**人間らしさ監査**：新着2本とも `また、／さらに、／そのため、／しかし、／一方、` が**0回**。
段落長 29〜252字（平均137）、H2セクション長 397〜2093字と**ばらつきが十分**。
「机が狭い人ほど一度立ち止まったほうがいい」等の明確な自己判断あり。編集不要と判断。

**失敗ラン**：`FAILURES.log` なし。`hc_runs.mjs 3` は **9/9 完走**。新規の未報告失敗なし。

---

## 2026-09-05（朝の執筆ジョブ）— push が一度 403 で弾かれた

29本目 `chintai-wanroom-shokusenki-koji-fuyou` の commit 後、`git push` が
<strong>403 / Permission to monoselect-lab/monoselect-lab.github.io.git denied to sheetwidget</strong> で失敗した。

原因：`gh auth status` を見ると github.com に2アカウント（`sheetwidget` と `monoselect-lab`）が
ログインしており、<strong>アクティブが sheetwidget に切り替わっていた</strong>。
git の credential helper が `gh auth git-credential` なので、アクティブ側のトークンが使われて弾かれる。
このランの外で切り替わったもので、原因は特定できていない。

対処：`gh auth switch --user monoselect-lab` でアクティブを戻してから push（成功）。
アクティブは monoselect-lab のままにしてある（毎日のジョブが期待する状態のため）。

→ **運用者へ**：他の作業で `gh auth switch` を使ったら、公開ジョブの前に
`gh auth status` のアクティブが monoselect-lab に戻っているか確認してください。
ヘルスチェック側に「アクティブアカウントが monoselect-lab か」の1行チェックを足す価値がありそうです
（`gh auth status` は10日連続greenだったが、**見ていたのはログインの有無だけでアクティブ側は見ていなかった**）。

---

## 2026-09-05（ヘルスチェック）— Bingスポットチェックが4度目の死。内部リンクの入次数ゼロを解消

### 指標（14日連続で不変）

| 項目 | 本日 | 前日 |
|---|---|---|
| 記事数 | 29本 | 29本（+0：夜のジョブ前） |
| インデックス | **1/32 URL**（トップのみ） | 1/30 |
| Search Console | クリック0 / 表示0 / クエリ0 | 同左 |
| sitemap `lastDownloaded` | **NEVER** | NEVER |

`hc_sitemap` は再送信を見送り（未ダウンロード＆pending のため、再送は順番待ちをリセットするだけ）。
IndexNow は32URLを送信し 200。

### Bingスポットチェックを廃止した

これで**4つ目**の照会方法が同じ壊れ方をした。`site:`（08-28死）→ ブランド完全一致（09-04死）→
**そして09-04に代替として採用したばかりの「本文の一意な文の完全一致」も、2回目の実行で死んだ。**

- 「シンクの縁が床から80cmだとすれば…」（55字・引用符付き）→ **マルゼン／LIXIL／IKEA のシンク製品ページ。「約101,000件」**
- 「賃貸ワンルームのミニキッチンで、コンセントにアース端子が…」（49字・引用符付き）→ **SUUMO等の姶良市の賃貸物件一覧。「約306,000件」**

インターネット上に1ページしか存在しない文に対して6桁のヒット数が出ている時点で、
Bingは引用符を無視して「シンク」「賃貸」まで**緩めている**。
つまりこの検査はもう「インデックスされていない」と「Bingが引用符を無視した」を区別できない。
**区別できない検査は、検査が無いより悪い**（ブロッカーの誤った裏付けを毎日生産してしまう）。
`health_check_prompt.txt` から削除し、代わりに**運用者への依頼**（Bing Webmaster Tools への登録）に置き換えた。
GSCが検証済みなので "Import from Google Search Console" で2クリックで済み、
推測ではない実インデックス数・クロール履歴・IndexNowの受理状況が見られるようになる。

### `gh auth status` の穴を塞いだ

09-05朝の publish が 403 で落ちた件（アクティブアカウントが `sheetwidget` に化けていた）を受けて、
チェックリストに**アクティブアカウントの明示アサート**を追加した。
`gh auth status` は10日連続greenだったが、見ていたのは「ログインがあるか」だけで、
実際に push に使われる**アクティブ側**は一度も見ていなかった。緑の検査が緑のまま実害を通した典型。
本日のアクティブは `monoselect-lab` で正常。

### 内部リンク：入次数ゼロが5本あった（4日ぶりのパス）

`related:` は全29本に付いていて「リンク孤児ゼロ」は満たしていたが、**被リンク側を数えていなかった**。
数えたら**入次数0が5本**、いずれも直近に公開した記事だった（新しい記事に古い記事から入る導線が無い）。
一方 `wanroom-shigoto-neru-basho-shikiri` には7本集中。10か所を貼り替え：

- 電源タップ／ブレーカー → **食洗機**（記事本文が「電源タップの1,500W上限」を相互参照している）
- デスクライト／Web会議の顔の明るさ → **西日**（逆光・映り込みは西日記事の主題そのもの）
- Web会議の顔の明るさ／ノートPCスタンド → **カメラ目線の高さ**
- 布団のカビ／秋の花粉 → **ダニ死骸**（アレルゲンで地続き）
- 床キズ／間仕切り → **突っ張り棒・転倒防止**（原状回復まわり）

結果：**入次数ゼロ 0本、最大7→6、最小1**。`check:links` 29記事33ページ green。

### 価格ドリフト：真陽性1件

`hc_price` が `chintai-shinya-dakenon-taisaku` を DRIFT 判定。記事を開いて確認したところ真陽性だった。
サンワダイレクトのキーボードマットを**記事中5か所で「1,500円」**と書いていたが、実売は
**Mサイズ¥2,280／Sサイズ¥1,980**。導入部の「まず1,500円のマットを敷いてください」、
比較表の `¥1,500〜2,000`、結論部の「まず1,500円のマットで切り分ける」をすべて実勢に修正し、
`※価格は…時点` を9月5日に更新した。33%のずれで、**この記事の入口CTAそのもの**なので実害がある。

なお同記事の REALFORCE R3SC11 ¥98,000 は 09-04 に対応済みで、記事側が
「今は買い時ではありません」と明記しているため DRIFT ではない（意図した表示）。

### その他の検査（すべて緑）

`npm run build` 32ページ／`check:rendered` 32ページ／`check:links` 29記事33ページ／
`hc_tables` **35/35**／`hc_runs.mjs 3` **9/9完走**／`FAILURES.log` なし。
390px実測：`proseW 358`・`docScrollW 390`（横漏れなし）・本文フォント**16.5px（61段落中59）**。
※初回の計測は `document.querySelector('p')` が広告表記（14px）を拾っていた。
本文段落を数えて16.5pxを確認するまでは「小さい」という読みは保留すべきだった。
スクショ目視（トップ＋新着2本）：比較表のsticky1列目・価格列とも正常、崩れなし。

**収益性**：Amazonリンク**155本すべて** `tag=monoselectlab-22` 付き（欠落0）、
`rel="sponsored nofollow noopener"` も **155/155で一致**。
`hc_asin` は **35 ASIN（変更5記事ぶん26 ＋ ローテーション15、重複除く）を 35/35 生存・商品名一致**。
ローテーションオフセット 75 → 90（全133 ASIN）。
`hc_price` は 71/72 が印字価格の±30%以内、唯一のDRIFTが上記＝真陽性。43 ASIN は価格キャッシュ未到達。

**人間らしさ監査**：新着2本とも `また、／さらに、／そのため、／一方、` が**0回**。編集不要。

### 今日できたバックリンク側の一手

GitHubプロフィールリポジトリ `monoselect-lab/monoselect-lab` の README にある「最近の記事」が
**8月28日時点の4本のまま**だったので、最新6本＋記事一覧リンク＋総数29本に更新して push した。
github.com はGoogleが常時クロールしているドメインなので、
**このサイトに向いた数少ない実在のリンク**であり、鮮度を保つ価値がある。
プロフィールの Website 欄（`blog`）は**空のまま**にせざるを得なかった：
`gh api -X PATCH /user` に `user` スコープが必要で、現在のトークンに無い。
運用者が `gh auth refresh -h github.com -s user` を一度実行すれば以後は自動で埋められる。
